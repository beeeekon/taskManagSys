services:
  db:
    image: postgres:16
    container_name: postgres_db
    environment:
      POSTGRES_USER: astalavista
      POSTGRES_PASSWORD: 13ReasonsWhy
      POSTGRES_DB: task_management
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
